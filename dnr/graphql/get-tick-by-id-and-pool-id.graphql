query GetTickData($poolId: ID!, $tickId: Bytes!) {
  pool(id: $poolId) {
    id,
    collateralToken {
      name,
    },
    currencyToken {
      id,
      symbol
    },
    ticks(where: {id: $tickId} ) {
      limit,
      duration,
      rate,
      raw,
    }
    deposits {
      depositedAmount,
      shares,
      account,
      tick {
        id,
      }
    }
  }
}